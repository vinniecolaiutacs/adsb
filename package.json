{
  "name": "adsb-481",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "build": "yarn run build-backend && yarn run build-frontend",
    "build-frontend": "cd front-end && yarn install && yarn link @angular/cli && ng build",
    "build-backend": "yarn install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.7",
    "express": "^4.18.3",
    "path": "^0.12.7"
  }
}